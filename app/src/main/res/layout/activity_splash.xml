<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootSplash"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_splash_gradient">

    <!-- Top Ambient Glowing Aura -->
    <View
        android:id="@+id/viewAura"
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:background="@drawable/bg_splash_aura"
        app:layout_constraintBottom_toTopOf="@+id/layoutBrandCenter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/layoutBrandCenter" />

    <!-- Center Brand Container -->
    <LinearLayout
        android:id="@+id/layoutBrandCenter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.42">

        <!-- Animated Logo Container -->
        <FrameLayout
            android:id="@+id/frameLogoContainer"
            android:layout_width="140dp"
            android:layout_height="140dp">

            <!-- Glowing Halo behind Logo -->
            <View
                android:id="@+id/viewLogoGlow"
                android:layout_width="140dp"
                android:layout_height="140dp"
                android:background="@drawable/bg_logo_glow" />

            <!-- Brand Logo Mark ("محفظتك") -->
            <ImageView
                android:id="@+id/ivSplashLogo"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/app_name"
                android:src="@drawable/ic_urwallet_logo" />
        </FrameLayout>

        <!-- Main Arabic App Name -->
        <TextView
            android:id="@+id/tvAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:fontFamily="sans-serif-black"
            android:letterSpacing="0.02"
            android:shadowColor="#8000E5FF"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="14"
            android:text="@string/app_name"
            android:textColor="#FFFFFF"
            android:textSize="36sp"
            android:textStyle="bold" />

        <!-- English Brand Identifier -->
        <TextView
            android:id="@+id/tvAppEnglish"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.2"
            android:text="@string/app_name"
            android:textColor="#80D8FF"
            android:textSize="13sp"
            android:textStyle="bold" />

        <!-- App Tagline -->
        <TextView
            android:id="@+id/tvAppTagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:fontFamily="sans-serif"
            android:text="@string/app_tagline"
            android:textColor="#B0BEC5"
            android:textSize="15sp" />
    </LinearLayout>

    <!-- Bottom Footer Status Container -->
    <LinearLayout
        android:id="@+id/layoutFooter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="44dp"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <!-- Modern Pulse Progress Indicator -->
        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/progressLoading"
            android:layout_width="120dp"
            android:layout_height="3dp"
            android:indeterminate="true"
            app:indicatorColor="#00E5FF"
            app:trackColor="#2000E5FF"
            app:trackCornerRadius="2dp" />

        <!-- Subtitle -->
        <TextView
            android:id="@+id/tvAppSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:fontFamily="sans-serif-light"
            android:letterSpacing="0.08"
            android:text="@string/splash_tagline"
            android:textColor="#78909C"
            android:textSize="12sp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
