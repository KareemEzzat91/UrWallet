<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".UrWalletApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UrWallet">

        <!-- Splash: LAUNCHER, reads onboarding state and routes -->
        <activity
            android:name=".features.onboarding.presentation.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.UrWallet.Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding: 3-slide value proposition -->
        <activity
            android:name=".features.onboarding.presentation.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.UrWallet.Onboarding"
            android:launchMode="singleTop"
            android:noHistory="true" />

        <!-- Wizard: 5-step Smart Savings Wizard -->
        <activity
            android:name=".features.onboarding.presentation.wizard.WizardActivity"
            android:exported="false"
            android:theme="@style/Theme.UrWallet"
            android:windowSoftInputMode="adjustResize"
            android:noHistory="true" />

        <!-- Main app shell (tabs) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.UrWallet"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize" />

        <!-- App Lock: PIN & Biometric Screen -->
        <activity
            android:name=".features.security.presentation.lock.AppLockActivity"
            android:exported="false"
            android:theme="@style/Theme.UrWallet"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- Phase 12 Receivers -->
        <receiver
            android:name=".features.notifications.data.receiver.DailyReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".features.notifications.data.receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for Sharing Backups & CSV Exports -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
